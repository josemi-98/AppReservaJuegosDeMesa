
{% extends 'base.html.twig' %}
  
{% block title %}Nuevo juego{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Crear un nuevo juego</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_juego_index') }}"> 
                    Mantenimiento juego
                </a>
            </div>
            <div class="card-body">
                 {{ include('juego/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
